package twitter

/*
func TestNew(t *testing.T) {
	//urls := `https://twitter.com/asahi_kokusai`

	tw, err := New("",
		"",
		"2946383622-",
		"",
		"socks5://127.0.0.1:1086")
	if err != nil {
		t.Error(err)
		return
	}

	u, err := tw.GetUserByUsername("asahi_kokusai")
	if err != nil {
		t.Error(err)
		return
	}

	fmt.Println(jsons.MarshalStringDefault(u, false))
	sinceTime := clock.Sub(time.Now(), clock.Day*10)
	tws, err := tw.GetTweetsEx(u.Id, &sinceTime, nil)
	if err != nil {
		t.Error(err)
		return
	}
	for _, v := range tws {
		fmt.Println(jsons.MarshalStringDefault(v, false))
	}
}
*/
